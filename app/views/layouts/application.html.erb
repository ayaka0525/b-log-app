<!DOCTYPE html>
<html>
  <head>
    <title>BLogApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%# application.scssからインポートしている%>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%#表示領域を指定%>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%# bootstrapでアイコン表示のため %>
    <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet" crossorigin="anonymous">
  </head>

  <body>
  <header class="header">
  <div class="header_logos">
  <!--indexにいたら、戻るボタンを表示しない[!]を頭につけてそれ以外の条件という意味になる-->
  <% if !current_page?(root_path) %>
  <%= link_to :back do %>
  <%= image_tag 'back.svg', class: 'header_back' %>
  <% end %>
  <% end %>
      <p class="header_pageTitle">B-log-app</p>
      </div>
      <div class="dropdown">
        <%= image_tag 'default-avatar.png', class: 'header_avatar dropbtn' %>
        <div class="dropdown-content">
          <a href="#">プロフィール</a>
          <a href="#">ログアウト</a>
        </div>
      </div>
    </header>

    <% if flash.present? %>
    <div class="flash">
    <!-- flashはハッシュなのでeachする時'key','value'使える-->
        <% flash.each do |key, value| %>
          <div class="<%= key %>"><%= value %></div>
        <% end %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
